# مِرسال فاكتيف — نسخة الموقع

موقع ثابت (بدون خادم) يرفع فيه المدرب ملف الإكسيل، فتظهر له قائمة المشتركين مع الرسالة الجاهزة
لكل واحد، ويفتح محادثة واتساب والرسالة مكتوبة بضغطة واحدة.

**الملف يُقرأ داخل متصفح المدرب فقط ولا يُرفع إلى أي خادم** — لا أسماء ولا أرقام تغادر جهازه،
ولا يوجد ما يُخترق لأن الموقع صفحات ثابتة بلا قاعدة بيانات.

---

---

## كيف يستخدمه المدرب

1. يفتح الرابط.
2. **يختار ملف الإكسيل** (أو يسحبه على الكمبيوتر) — أي اسم، وأي عدد أوراق.
3. يختار الورقة إن كان الملف فيه أكثر من ورقة.
4. لكل شخص: **«فتح واتساب والرسالة جاهزة»** ← يفتح واتساب والرسالة مكتوبة ← يضغط إرسال ← يرجع للموقع، والتالي جاهز.
5. في النهاية: **«نسخ عمود الحالة للإكسيل»** ويلصقه في عمود الملاحظات (نفس ترتيب الصفوف).

التقدّم محفوظ في متصفح المدرب، فلو أغلق الصفحة وفتحها يكمل من حيث وقف.
الضغط على أي اسم في القائمة يفتح بطاقته مباشرة، حتى لو كان قد أُرسل.

---

## اختيار طريقة الفتح (القائمة في أعلى الصفحة)

| الخيار | متى | ماذا يحدث |
|---|---|---|
| **واتساب بزنس (أندرويد)** | الافتراضي على أندرويد | يفتح **تطبيق واتساب بزنس تحديدًا** ولو كان العادي مثبّتًا معه، والموقع يبقى مفتوحًا خلفه |
| **تطبيق واتساب** | الافتراضي على آيفون | يفتح تطبيق واتساب مباشرة. على آيفون لا توجد طريقة رسمية لإجبار نسخة البزنس دون العادية — النظام يفتح الافتراضي عنده |
| **واتساب ويب (كمبيوتر)** | الافتراضي على الكمبيوتر | يفتح `web.whatsapp.com` في تبويب مع الرسالة جاهزة |

الاختيار يُحفظ لكل مدرب على جهازه.

**نصيحة لمستخدمي الآيفون:** إن كان عندهم واتساب عادي وبزنس على نفس الجهاز، الأفضل أن يبقى **البزنس** هو المثبّت
أو أن يستخدموا زر **«نسخ الرسالة»** ثم يفتحوا المحادثة من داخل تطبيق البزنس ويلصقوا.

---

## الأعمدة والتعريب

- الأعمدة تُكتشف تلقائيًا من صف العناوين: `اسم` / `رابط واتساب` / `الرقم` / `الدولة` / `الباقة` / `المصدر` / `الرسالة الجاهزة`.
- الأرقام تُنظَّف ويُضاف مفتاح الدولة من عمود الدولة (965 / 974 / 971 / 966 / 973 / 968)، والصفوف بلا رقم صالح تُتجاهل.
- الاسم الإنجليزي يُستبدل بالعربي داخل الرسالة من `names_ar.json`، والاسم غير الموجود يُعرَّب تقريبيًا مع تنبيه **«راجع الاسم»**.
- المدرب يقدر يعدّل الاسم أو نص الرسالة قبل الإرسال، وتتحدّث الرسالة فورًا.

**لإضافة أسماء للقاموس:** افتح `names_ar.json` في GitHub ← أيقونة القلم ← أضف سطرًا ← Commit:

```json
"meshari": "مشاري",
"jarah": "جراح"
```

الجميع يستفيد من الإضافة في المرة القادمة التي يفتحون فيها الموقع.

---

## الفرق عن النسخة المحلية على جهاز سامر

| | الموقع | الأداة المحلية |
|---|---|---|
| التثبيت | لا شيء — رابط فقط | Python + مجلد + إضافة كروم |
| مصدر الملف | يرفعه المدرب في كل مرة | أحدث ملف في مجلد `inbox` |
| السجل ومنع التكرار | لا يوجد (كل مدرب على جهازه) | `logs` + تنبيه التكرار خلال ١٤ يومًا |
| الفتح الداخلي بلا إعادة تحميل | على الكمبيوتر فقط مع الإضافة | نعم |
| الموبايل | ممتاز — التطبيق يفتح مباشرة | غير مدعوم |
